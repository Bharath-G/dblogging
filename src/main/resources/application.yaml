spring:
  application:
    name: dblogging
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:XE
    username: system
    password: system123
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.OracleDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
